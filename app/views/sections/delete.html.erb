<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back_link') %>

<div class="sections new">
<h2>Update Section</h2>

<%= form_for(:section, :url => {:action => 'destroy', :id => @section.id}) do |f| %>

	<p>Are you sure you want to permanently delete this section?</p>

	<p class="reference-name"><%= @section.name%></p>

	<div class="form-buttons">
		<%= submit_tag("Delete this section!") %>
	</div>
<% end %>

</div>